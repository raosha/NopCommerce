@model Nop.Plugin.Misc.Ebay.Models.EbayConfigurationViewModel

@{
    //page title
    ViewBag.PageTitle = "Ebay Configurations";
    Layout = "_AdminLayout";
    Html.SetActiveMenuItemSystemName("Ebay Configuration");
}

<form asp-controller="EbayConfiguration" asp-action="Create" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            Add Configuration
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">Back to Configuration</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>